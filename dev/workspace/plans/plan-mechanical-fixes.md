# Plan: Mechanical Fixes (orphaned defects)

Status: Ready — any session, low risk. Sources: report-grammar-typos.md, report-prose-review.md. Only defects NOT owned by another plan are listed; everything else rides with its owning plan (noted there).

## Work

- `docs/drafthorse/framework/conventions.md:3` — "all equal. how much any one weighs" → capitalise "How"; consider restoring the lost opening description ("flat, open bucket"; "each entry is a rule paired with the test that makes it actionable" — note the test-pattern phrasing is now dated per the Gates decision; restore the bucket line only).
- `docs/drafthorse/framework/notation.md:7` — prose-review flagged the weakened thesis ("an executable marking" vs the old "bold + CAPS = a rule the agent must obey; the only executable marking") — restore the sharper claim if intended.
- `docs/drafthorse/framework/handover.md:52` — "self contained" → "self-contained"; ":48 "reviwer" → "reviewer" (if not already consumed by the handover.md rewrite in [plan-handover-location.md](plan-handover-location.md)).
- `docs/drafthorse/framework/steps.md:306-ish` — prose-review noted the anatomy line lost "nothing else" (own-step-only constraint on finished conditions); verify the Conditions section still carries it, restore if lost.
- `docs/drafthorse/framework/steps.md` gate paragraph — prose-review noted the rewrite dropped the rubber-stamp/launder guard; the Gates decision (single statement) supersedes — confirm no restoration wanted.
- [x] `docs/drafthorse/framework/scaffold.md` permissions bullet — FIXED BY THE USER during the scaffold.md session. The bullet ended mid-sentence ("…rather than held globally. This also ensure grants"); the truncated clause was struck. Found mid-session, owned by no plan.
- ~~`extensions/skills/classroom/SKILL.md` Terms `:` prefix — owned by [plan-reference-notation.md](plan-reference-notation.md); listed here only so it isn't forgotten if that plan defers it.~~ **VOID** — the `:` prefix is deprecated framework-wide, not restored. See [plan-terms-notation.md](plan-terms-notation.md); classroom is a de-prefixing site in wave 3.

## Dependencies

None — but check each item's owning plan hasn't already consumed it before editing (several handover.md/conventions.md defects vanish inside those rewrites).
