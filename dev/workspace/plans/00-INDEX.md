# DraftHorse Spec Upgrade — Plan Index

Branch: `drafthorse-spec-upgrade`. One plan doc per unified concept/TODO; this index is the cross-plan map — execution waves, shared-file contention, and dependencies. Source material: the two discussion docs (decisions of record) and five review reports (finding inventories) in this folder. Settled with NO work: Dynamic Improvement (concept 9 — stays as written in conventions.md).

## Working context

The three surfaces this upgrade operates on:

- `docs/drafthorse/` — the framework (authoring spec): `framework/{scaffold,conventions,steps,notation,references,handover,environments→surfaces}.md` + `framework/README.md`, and `drafthorse-spec-check.md` (the audit spec; its frontmatter `update_instructions` govern the saddler). The former `template/SKILL.md` was deleted as a redundant duplicate of the skill's `assets/SKILL-template.md` (§17); that asset is now the sole SKILL template.
- `extensions/skills/drafthorse/` — the drafthorse skill (the convert/author workflow): `SKILL.md`, `assets/SKILL-template.md`, `assets/HANDOVER-template.md`, `references/` (condition-writing, step-splitting, collecting-references).
- `extensions/agents/drafthorse-saddler.md` — the audit subagent, REGENERATED from drafthorse-spec-check.md, never hand-edited.

Downstream consumers touched by the migration wave: the DraftHorse-format skills (`extensions/skills/{classroom,versioning,git-box,agent-commit,agent-push,agent-switch}/`) and any agent docs carrying the preamble/step anatomy.

## Status at a glance

- **ALL PLANS DONE — waves 0–4 closed. The framework upgrade is content-complete.** Only unshipped: the release (wave 5 below) and the classroom document fixes ([plan-classroom-saddler-fixes.md](plan-classroom-saddler-fixes.md), deferred, classroom-scoped).
- Open decision carried forward (not blocking): the grants-transfer claim is unverified in 5 sites — see [plan-spec-check-saddler.md](plan-spec-check-saddler.md).
- On hold (1): [plan-drafthorse-rule.md](plan-drafthorse-rule.md).

## Execution waves

Wave 0 — discussions (CLOSED; all four settled, each plan now Ready):

- [x] [plan-preamble-rewrite.md](plan-preamble-rewrite.md) — SETTLED; now Ready. Final preamble text decided; its steps.md work (canonical copy, in-play/active synonym, step usage patterns catalogue) joins the steps.md session in wave 1, and the copy-sweep rides wave 3. Unblocks the handover template.
- [x] [plan-utilities-term.md](plan-utilities-term.md) — SETTLED; now Ready. Five utilities (handover is a dynamic reference, not a peer); "segment" dropped; house metaphor is the scaffold.md definition sentence. scaffold.md bulk runs in its own session; its one-line de-segmenting edits join the notation/conventions/handover sessions. Unblocks the references.md opening noun.
- [x] [plan-config-wiring.md](plan-config-wiring.md) — SETTLED; now Ready. Config wiring is NOT a frontmatter concept (declared in plugin.json; cannot substitute into frontmatter at all) — scaffold.md's bullet is DELETED, not uncommented, and the concept is recatalogued as a "User configuration" dynamic reference. Also found a bug in references.md's dynamic-family definition.
- [x] [plan-handover-template.md](plan-handover-template.md) — SETTLED; now Ready. Turned out to be more than tuning: the handover-variant preamble is final, handover.md's "same universal preamble" claim is FALSE and gets rewritten, and handover globals are RE-SCOPED to the parent step (reversing the whole-run rule). Feeds two spec-check changes.

Wave 1 — framework docs (CLOSED; all four sessions done). Grouped by contended file; each group was one session to avoid conflicting edits:

- [x] conventions.md session (DONE): [plan-conditions-carry-routing.md](plan-conditions-carry-routing.md) + [plan-steps-are-universal.md](plan-steps-are-universal.md) (paired bullets) + [plan-gates-are-compound.md](plan-gates-are-compound.md) + [plan-sharp-prose.md](plan-sharp-prose.md) + [plan-naming-not-explaining.md](plan-naming-not-explaining.md) + the conventions.md items of [plan-half-applied-dispositions.md](plan-half-applied-dispositions.md), [plan-surfaces-doc.md](plan-surfaces-doc.md) (Sub Agents bullet removal) and [plan-utilities-term.md](plan-utilities-term.md) (line 32 "data segment") + duplicate-bullet cleanup. This single session resolves every TODO block in conventions.md.
- [x] steps.md session (DONE): [plan-half-applied-dispositions.md](plan-half-applied-dispositions.md) (disposition catalogue) + [plan-decision-slot.md](plan-decision-slot.md) + [plan-preamble-rewrite.md](plan-preamble-rewrite.md) steps.md items (canonical preamble copy, in-play/active synonym, step usage patterns catalogue — the sibling of the disposition catalogue) + steps.md items of universal/routing/gates plans. Write all prose using the NEW machinery heading names.
- [x] notation.md session (DONE; also owned references.md): [plan-condition-links.md](plan-condition-links.md) + [plan-reference-notation.md](plan-reference-notation.md) + the notation items of [plan-handover-citation.md](plan-handover-citation.md) and [plan-machinery-headings.md](plan-machinery-headings.md) and [plan-utilities-term.md](plan-utilities-term.md) (line 22 "segment dividers" → dividers) + [plan-config-wiring.md](plan-config-wiring.md) references.md items (new "User configuration" dynamic-reference entry; FIX the dynamic-family shape sentence, which is false for Data load as well). Also took the framework-wide bare-filename link sweep (all 16 sites across scaffold/conventions/steps/references/notation/handover — the convention is set here, so it landed in one mechanical pass rather than waiting for wave 3) and the framework half of the NEW [plan-terms-notation.md](plan-terms-notation.md), including scaffold.md:43.
- [x] handover.md session (DONE — wave 1 is closed): handover.md items of [plan-handover-location.md](plan-handover-location.md), [plan-frontmatter-stamp.md](plan-frontmatter-stamp.md), [plan-utilities-term.md](plan-utilities-term.md) ("Steps segment" — the last "segment" in the framework), and [plan-handover-template.md](plan-handover-template.md). Also took `framework/README.md` (`type: handover` + a "five frame parts" utilities miss — no session owned that file) and HANDOVER-template.md (pulled forward from wave 3: its globals comment was actively teaching the reversed rule). Deliberate scope limit: parent/child vocab was applied ONLY to sentences a plan had me rewriting, so handover.md still carries "master step" in its untouched paragraphs — wave 3 finishes it. Two findings recorded: step-splitting.md has no globals rule to align (plan premise wrong), and the retired references/ carve-out sharpens the shared-worker gap for wave 4.
- [x] scaffold.md session (DONE): [plan-utilities-term.md](plan-utilities-term.md) — definition sentence, five utilities, de-segmenting — plus [plan-config-wiring.md](plan-config-wiring.md) (Config wiring bullet deleted). Three amendments landed here: the house metaphor is NOT written into scaffold.md (reverses the wave-0 §13 decision — it was the reasoning that settled the word, not prose the document needs); the frontmatter concern count stays FOUR because [plan-frontmatter-stamp.md](plan-frontmatter-stamp.md)'s scaffold item (unassigned by this index — a gap) takes config wiring's place in the list; and scaffold.md's `type: handover` paragraph plus a machinery-heading inventory miss were corrected in passing.

Wave 2 — new docs:

- [x] [plan-surfaces-doc.md](plan-surfaces-doc.md) + [plan-child-skills.md](plan-child-skills.md) in one session (DONE — wave 2 is closed). environments.md `git mv`'d to surfaces.md and written whole: four entries in extraction-ladder order (Main Skill File, Handover Documents, Child Skills, Sub Agent), the child-steps/child-skills distinction stated once, the extraction ladder as a closing decision path, and the `${user_config.*}` mechanism per [plan-config-wiring.md](plan-config-wiring.md). Satellites: README.md index (five layers → six, surfaces marked as a different kind of doc — "read once the grammar is in hand"; "five utilities" is a different five and stands), scaffold.md Permissions bullet (comma splice fixed, reduced to one line + pointer), references.md wave-2 TODO closed with the surfaces link. Amendments and findings below.

  - Amendment: **the invocation lock is role-driven, not a child-skill trait.** git-box carries `disable-model-invocation: true` because that procedure requires the user to open it, not because it is a parent; a main skill file is a doorway suitable for either pathway. The Child Skills entry therefore teaches description-as-lock plus "the field that fits the skill's role" (`user-invocable: false` for calling-agent-only, `disable-model-invocation: true` for user-only), rather than naming one canonical lock.
  - Verified before writing: git-box bears out the grants-transfer claim. git-box grants `Skill(dev-tools:agent-commit|push|switch)` + `Agent(git-robot)`; git-robot holds only `Skill(agent-commit|push|switch), Bash`; each verb skill is `user-invocable: false` and holds its own narrow `Bash(git commit *)`-style grants. Written as framework fact on that evidence.
  - Already done by wave 1: conventions.md's "Sub Agents" trailing bullet was removed in the conventions session. No work remained for the [plan-surfaces-doc.md](plan-surfaces-doc.md) satellite item.
  - **RESOLVED — User configuration left the framework entirely** (amends [plan-config-wiring.md](plan-config-wiring.md), which had assigned it to surfaces.md). It is plugin wiring, not a delivery surface and not document grammar, so it now lives at `docs/user-configuration.md` alongside the repo's other plugin-authoring guides. `docs/claude_docs/plugin_reference.md` is named in it as the source of truth (it is vendored upstream material and wins on any disagreement; our page is a condensation that can drift when it is re-vendored). The `references.md:19` pointer was **deleted rather than repointed** — a pointer from the framework into a repo-specific guide would make `docs/drafthorse/` depend on this repository's layout, breaking its portability. The pointer was not earning its place anyway: references.md already states everything a document author needs (the value arrives resolved, the step never reaches for configuration, a step never receives a secret), while the syntax, substitution surfaces and keychain route are only actionable when writing `plugin.json` — a different author and a different document. Framework grep for `user_config` now returns nothing.
  - Final shape of surfaces.md (supersedes the draft described above): four self-contained entries — **Skill File**, **Sub Agent**, **Child Skills**, **Handover Documents** — each running what-it-is / how-it-behaves / **Use it when**. No extraction ladder and no decision funnel: selection lives inside each entry. Author's standing rule applied throughout — **each entry talks about itself and does not lean on another surface to make its point**; all cross-surface comparisons were cut. Known trade recorded: Child Skills and Handover are the two most easily confused (both extract conditional work from a parent) and the permissions contrast that separated them is gone; if that causes real confusion the fix is sharpening each entry's own trigger, NOT reintroducing comparisons. All named-skill examples (`git-box`, `git-robot`, the verb skills) were cut as unreachable meaning.
  - **URGENT for wave 3 — the drafthorse skill teaches spec-violating handovers.** `framework/handover.md` mandates three agreeing signals (the `harness-format: DraftHorse, Handover` stamp, a `-handover.md` suffix, skill-root location) and states outright: NEVER file a handover in `references/`. Every handover in the corpus violates all three — `extensions/skills/classroom/references/{setup,mark-review,media-processing,deliver-without-renderer}.md` each carry `type: handover` as their whole frontmatter and sit in `references/`. Worse, `extensions/skills/drafthorse/SKILL.md:32`, `:178` and `:247` still INSTRUCT the old form ("a `references/` file whose frontmatter is the single line `type: handover`"), as does `classroom/SKILL.md:351`. `assets/HANDOVER-template.md` already uses the new stamp, so the skill and its own template disagree. Wave 3's handover work is therefore larger than [plan-handover-location.md](plan-handover-location.md) records: it is four file moves/renames/restamps PLUS four instruction sites in the skill that currently teach builders to produce non-conforming documents.
  - **Agent bodies are DraftHorse documents — README.md:17's TODO is settled by the corpus.** Four of five files in `extensions/agents/` are full DraftHorse documents (global invariants, `# --- REFERENCES ---`, `# --- STEPS ---` with the verbatim preamble, `+`-prefixed steps carrying the contract headings, `# --- TERMS ---`): `breaking-change-detector.md`, `git-robot.md`, `course-researcher.md`, `drafthorse-saddler.md`; `doc-reviewer.md` is the outlier. Agent frontmatter (`tools`, `model`, `color`, `background`) stands in for skill frontmatter. This is NOT a fifth surface — it is a correction to the Sub Agent entry: the agent file IS the document, not a runner of one.
  - **Child Skills is evidenced at n=1 and the framework overclaims it.** The only case is git-box's three verb skills (`agent-commit`, `agent-push`, `agent-switch`) — one parent, one family, one motivation. The demonstrated trigger is PERMISSIONS: git-box holds only `Bash(git status *)` and deliberately holds none of the mutating git grants, which sit in the disjoint narrow grant sets of the three children. surfaces.md's claim of "fresh context, its own permissions, or both" is only half-evidenced — fresh context is a co-benefit in the one case, not a demonstrated trigger. Narrow the entry to what the corpus proves.
  - **OPEN question — redundant grant paths.** `git-box/SKILL.md` holds BOTH `Agent(git-robot)` and the three `Skill(dev-tools:agent-*)` grants, while `git-robot.md` separately carries the same three skills in its own `tools:`. Since a skill's grants transfer to the sub agent that invokes it, one of the two paths is redundant. surfaces.md currently asserts "the agent file itself need only grant the skills it dispatches" — an ideal the corpus does not follow. Establish which path is load-bearing before restating it as framework fact; the sentence is pulled from the draft until then.
  - Selection criteria the corpus actually demonstrates (grounding for the surfaces.md rewrite): **bulk alone does not trigger extraction** — `versioning/SKILL.md` is the largest skill at 397 lines and extracts nothing, being one linear always-run chain on one grant set. Handover's trigger is a triad, recurring cleanly across all four classroom cases: the work is CONDITIONAL, it needs the parent's live context, and it needs no grant the parent lacks. Sub agent's trigger is that the caller wants the result and not the transcript (token quarantine, background noise, an audit verdict), with per-agent `model` selection as a real secondary benefit no other surface offers.
  - Minor inconsistency noted, not fixed: `versioning/SKILL.md`'s config-discovery work (`references/config-template.md`, cited at `:126`) is conditional — it runs only when config is missing or broken — which is handover-shaped, but it was left as a plain reference. The corpus's one questionable surface call.
  - NEW problem found (reported, not fixed): `framework/README.md:17` carries a live TODO — "DraftHorse is often placed in agent body also, so default is not the only skill". An agent body is arguably a fifth surface, but it is outside this plan's decided four-entry scope. Needs a decision before wave 4.

Wave 3 — the migration pass (ONE atomic sweep across all skills/templates/agent docs; splitting it leaves mixed dialects):

- [x] [plan-machinery-headings.md](plan-machinery-headings.md) + [plan-parent-child-vocab.md](plan-parent-child-vocab.md) + preamble copy-sweep + [plan-frontmatter-stamp.md](plan-frontmatter-stamp.md) + [plan-handover-location.md](plan-handover-location.md) file moves + [plan-handover-citation.md](plan-handover-citation.md) + [plan-utilities-term.md](plan-utilities-term.md) stale terms + [plan-terms-notation.md](plan-terms-notation.md) de-prefixing — **ALL DONE**. Closing inventory is clean: every retired form returns zero across the migratable corpus (both frozen files excluded). Retired to zero: the five old machinery headings (including `#### Decision:` and the `Agent Decision:` casing variant), `master step` / `master document` / `sub-step`, `type: handover`, the `: **Term**:` prefix form, and `five parts` / `declaration segment` / `data segment`. Now in service: 20 files on `Start this step when these are true:`, 14 on `Suggested next actions:` and `Step invariants:`, 8 with `Agent decision:` blocks, 9 carrying `harness-format: DraftHorse, Handover`. `versioning/SKILL.md:180` deliberately untouched — "resolve the `../` segments" means path segments. Findings below.

  - **Two preambles, not one — and the sweep nearly got it wrong.** All 16 documents carried an identical old preamble (no drift, so a clean replace). The first pass matched on that string and therefore also hit the four classroom handovers, giving them the UNIVERSAL preamble — precisely the bug `handover.md:58` warns of, since the universal preamble routes a failed step to an error drain a handover does not have. Caught and corrected in a second pass. Final split: universal in 12 documents + `SKILL-template.md`; handover-variant in the 4 classroom handovers + `HANDOVER-template.md`. **Standing hazard for future sweeps: the classroom handovers answer most corpus-wide greps and almost always need different treatment from the skills beside them.**
  - **Method changed mid-wave at the author's direction: read each file, then edit per verified site.** Scripted regex is for measurement only (inventories, counts, proving a set empty), never for the edit. Two real failures drove this — a `sub-steps` → `child steps` rename produced "breaking into several child steps of its own" (a handover's steps are child steps *of the parent*, not of the handover; now "several steps of its own" in both `handover.md` and `step-splitting.md`), and the preamble mis-hit above. Where `Edit` with `replace_all` was used, every site was first enumerated with its owning step and confirmed to map uniformly.
  - **`handover.md:46` was CHANGED rather than propagated** (author's call). It required a five-clause identity paragraph; three of those clauses restate the reading model the handover-variant preamble now carries. It requires two clauses, and states explicitly that the identity paragraph never restates the reading model. The four classroom handovers were trimmed to match. `HANDOVER-template.md` was left at two clauses — it was already correct.
  - **The old location had a live broken citation, not just a mis-signal.** `setup-handover.md` cites `references/students/_template.md`. From its former home at `references/setup.md` that resolved to `references/references/students/…` — broken. From the skill root it resolves. Worth checking for the same pattern anywhere else a file moves.
  - `condition-writing.md` heading asymmetry, deliberate: `## Start conditions (…)` and `## Finished conditions (…)` keep their parenthetical machinery string, but the third is plain `## Suggested next actions` because the parenthetical would have duplicated the heading word for word. Also "a supervisory step spanning its **sub-steps**" → "its **inner steps**" — that is a supervisory step over ordinary steps, so "child steps" would have been actively wrong.
  - `step-splitting.md` gained a **handover-variant preamble** delta. The file catalogued five deltas and the preamble was not among them, despite being the difference most likely to produce a broken handover.
  - `collecting-references.md` — the retired `references/` carve-out is gone, replaced by the rule that a handover is never a reference, carries the three signals, and that a stamped file in `references/` is a signal mismatch and a defect. Closes the loophole recorded at [plan-spec-check-saddler.md](plan-spec-check-saddler.md).
  - `doc-reviewer.md` was in the migration set after all — it carried the old preamble and machinery headings. The wave-2 corpus report characterised it as "the outlier" that was not a full DraftHorse document; that was slightly wrong.
  - Cosmetic inconsistency noted, NOT fixed (no plan covers it): `course-researcher.md` and `breaking-change-detector.md` have no "Terms used in this agent:" lead line, while `git-robot.md` and `doc-reviewer.md` do.
  - **classroom release flag** (not a release): four files moved and renamed, four citations rewritten, 38+ headings, Terms rewritten. Independently versioned plugin — versioning is its own workflow.
- [x] [plan-mechanical-fixes.md](plan-mechanical-fixes.md) — DONE. The verify-first instruction paid off: of five open items, only ONE needed an edit. `conventions.md:3` capitalisation fixed (opening then rewritten by the user; bucket-line restoration declined). Two items were false alarms from the prose report — `handover.md`'s typos were already consumed by the wave-1 rewrite, and `steps.md`'s "nothing else" constraint was never lost (the report read the one-line anatomy summary at :50; the full constraint sits in the `## Conditions` prose below it). Two were decided as no-action: the `notation.md:7` sharper thesis is now FALSE (the same document makes `**OR these are true:**` executable, so bold+CAPS is not "the only executable marking"), and the `steps.md` gate paragraph is correctly superseded by the Gates decision. **Wave 3 is closed.**

Wave 4 — verification layer (strictly last):

- [x] [plan-spec-check-saddler.md](plan-spec-check-saddler.md) — **DONE. Wave 4 is closed; the framework upgrade is content-complete.** The spec-check was rewritten section by section against the migrated framework, the saddler regenerated from it (bodies diff identically — in lockstep), and the saddler run against migrated classroom as the acceptance test. Outcomes and corrections below.

  - Two decisions taken at the top (both were "decide here" items): the **grants-transfer claim stays unchanged in all 5 sites** (4 framework + spec-check) — unverified but not touched this wave, still logged for a future decision; the **shared-worker gap is resolved by a new `Authoring guides` reference check** — a guide that teaches how to do a step's work is legal data as long as it carries no steps with their own conditions. That legitimises `drafthorse/references/{collecting-references,step-splitting,condition-writing}.md` in place and gives the saddler a structural test instead of a standing false positive.
  - Spec-check changes landed: upstream-authority rule added to `update_instructions`; utilities terms (five utilities, data utility); five machinery headings + condition-links + terms form + handover-citation-form in Notation; config-wiring bullet DELETED and format-stamp added in Frontmatter; Handover Checks rewritten to the three-signal model with **parent-step-scoped** globals (NOT whole-run — the reversed rule from [plan-handover-template.md](plan-handover-template.md)); TWO preambles taught; half-applied recast as a disposition check; Decision→Agent decision with the named-fact ban; sharp-prose + naming added to Document-Wide; a new **`+Audit the Handovers`** step; the `+Check the Frame`/handover-audit contention resolved (frame no longer claims Handover Checks); the scenario-walk gated so a no-handover set does not deadlock.
  - **CORRECTION — [plan-frontmatter-stamp.md](plan-frontmatter-stamp.md) was NOT done in wave 3, contrary to the wave-3 entry above.** Only the handover half (`type: handover` → the subtype stamp) had been applied; the plain `harness-format: DraftHorse` stamp was on NO skill or agent, while scaffold.md and the new spec-check both require it. Fixed here: stamp applied to all 12 unstamped documents (drafthorse/classroom/versioning/git-box SKILLs, the 3 git verb skills, 4 agents, SKILL-template) plus the saddler via regeneration. The spec-check itself is deliberately UNSTAMPED — it is an authoring source, never executed or installed, and stamping it would make the auditor's own source appear as an auditable document.
  - **The acceptance test worked as designed — it found SIX defects in the checker, one of them introduced this wave, all now fixed and the saddler re-regenerated:** (1) my half-applied check described start conditions but tested the error step — two different objects; (2) `Never names its parent` false-positived on a parent naming a handover's output in the same sentence as the fold-in (collided with `Parent step owns the logic`) — now judged by position relative to the fold-in; (3) `Reduced audit profile` vs Terms — handovers now explicitly inherit the parent's Terms; (4) `In-block labels` never defined "block" — now defined; (5) `No dead weight` silent on folder-level citations — now covers conditionally-reached files; (6) `Exit steps present` quoted the error condition as fixed text — now required sense, not text.
  - Classroom's OWN findings (verdict `revise`, 12 findings — routing deadlocks, a stall, a mark→record contradiction, dead references, empty channel data) are document defects, not framework, and are recorded in [plan-classroom-saddler-fixes.md](plan-classroom-saddler-fixes.md) for a later classroom-scoped task.

Wave 5 — RELEASE (not part of the upgrade's content; the upgrade is done, but nothing has shipped):

- [ ] The migrated documents live only in the repo. The harness loads `agent-tools:drafthorse-saddler` and the drafthorse skill from the INSTALLED plugin cache, which is still the pre-upgrade release — so the live saddler audits against the OLD spec until a new version ships. Cut releases via `/dev-tools:versioning` (its own workflow): **agent-tools** (drafthorse skill + saddler — the framework upgrade's shippable surface) and, separately, **classroom** (four handover files moved/renamed, headings, Terms — plus, if sequenced together, the [plan-classroom-saddler-fixes.md](plan-classroom-saddler-fixes.md) document fixes). Push before install. The true end-to-end acceptance test (saddler-through-the-plugin against a migrated skill) can only run AFTER the agent-tools release.

## Contended files (why the waves exist)

- conventions.md — 7 plans touch it → one session.
- steps.md — 6 plans → one session (plus migration ride-through).
- notation.md — 4 plans → one session.
- handover.md — 3 plans → one session.
- Every skill/template (preamble + headings + vocab + stamp) → the single wave-3 pass.
- spec-check + saddler — write-once at the end; NEVER edited per-plan.

## Cross-plan rules for executing agents

- Decisions of record live in new-concepts-discussion.md and todos-discussion.md — a plan summarises its decision, but on any doubt the discussion doc wins; do not re-litigate decisions.
- Write all new prose with the post-migration vocabulary (new heading names, parent/child, DraftHorse casing) even before wave 3 sweeps the old copies.
- Do not edit drafthorse-saddler.md directly, ever — regenerate from the spec-check.
- **The spec-check and saddler are knowingly STALE until wave 4.** This is by design: the framework moves in waves 1–3 and is folded into the spec-check last. Consequences, binding on every executing agent:
  - The framework is the authority. Where a check disagrees with a framework doc, the CHECK is wrong.
  - NEVER edit a framework doc, skill, or template to satisfy a spec-check or saddler finding. That is how `handover.md` acquired its false "same universal preamble" claim — a document made wrong to silence a flag.
  - Saddler findings produced before wave 4 are not authoritative. Do not run it as a gate; the acceptance-test run comes after regeneration.
  - Record any check/framework disagreement against [plan-spec-check-saddler.md](plan-spec-check-saddler.md) instead of acting on it.
- classroom is an independently versioned plugin: wave-3 changes to it warrant a release flag, not a release (versioning is its own workflow).
- Report NEW problems found mid-plan; do not improvise fixes outside the plan's scope (the Dynamic Improvement ethos applies to this upgrade itself).
