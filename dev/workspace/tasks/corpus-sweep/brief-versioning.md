# Target: extensions/skills/versioning/SKILL.md

Read also: `dev/branches/drafthorse-spec-upgrade/plans/versioning-drafthorse-fixes.md` — the approved fix plan (#40). Its ticked fixes (1, 2, 3, 4, 5, 6, 7, 9, 12) apply here; Borderline 8 stays as-is (rationale kept); Fixes 10 and 11 land only through what Fix 12 and Fix 4's ticked text carry of them, as specified below. Fix 2's proposed condition texts predate the de-hold retirement — the shapes below supersede them where they differ.

## Global invariants

- Delete the third invariant whole ("**NEVER** update a unit's release artifacts … before the user has explicitly approved …") — Fix 6; `+Execute`'s start condition is the mechanism.
- Keep the pathspec invariant whole, rationale sentence included (Borderline 8: kept).
- Add: `**ALWAYS** release the chosen units one at a time, in the order the user chose.` — this rehomes `+Range`'s slot fact. It is global, not a `+Range` step invariant: the ordering binds during the release loop, after `+Range` has finished and its step invariants would have lapsed.
- Add: `**NEVER** resume a failed run. Report what happened and what state the repository is in, end the skill, and let the user repair and re-invoke.` — Fix 12.

## Per step

- **+Dispatch** — start: `- the skill has been invoked` (trim the "not yet identified" de-hold). Finished (list): config read; unit(s) identified in the config from `[Request](#request)`, or every unit when the invocation was bare. No OR block — the no-config state is `+Setup`'s to claim, and `+Dispatch` completes once a config exists.
- **+Setup** — declare `**Dormant step**`. Start per the plan's Fix 2 block (list + OR block). Finished: config covering every requested unit on disk; user has approved it. Slot ("Carry on with the release.") deleted — HINT.
- **+Preflight** — start: `- the requested unit(s) are identified in a config` (trim the verified-this-run de-hold; drop the repaired-precondition OR branch — Fix 12). Finished: three items (tree clean / branch is remote default / gh authenticated where a unit enables github-release) — Fix 3. Engagement: "Report any failing precondition to the user." — delete "and offer to fix it with them; after a fix, verify again" (Fix 12). Step invariant stays.
- **+Range** — start: `- the release preconditions have been verified this run` (trim the no-computed-range clause). Finished: five items per Fix 1/3 (range computed per targeted unit; current version recorded; no-commits units recorded "nothing to release"; after a bare invocation the user has chosen; unchosen surveyed units recorded as not selected). Agent decision block stays. Slot deleted (its ordering fact is now the global invariant). Engagement `#### Path errors`: keep the diagnostic half (why verify-first tells a wrong path from a quiet unit); replace the disposition tail ("…and leave the unit without a computed range") with reporting shape — state which path git rejected and how it was resolved (symlink target converted wrongly, stale target, config path that no longer exists); the step then cannot finish and the error step claims it.
- **+Breaking Changes** — declare `**Looping step**`. Start: keep the per-unit discriminator pair as a two-item list (legitimate loop shape). Finished: three items per Fix 2.
- **+Propose** — declare `**Looping step**`. Start: three items — unit has commits in range; a breaking-change verdict is recorded for it; neither an approval nor a decline stands for it (drop the withdrawn-approval elaboration — Fix 12 calls it a no-op restatement). Finished: four items per Fix 2/7 — compound gate, approval last.
- **+Execute** — declare `**Looping step**`. Start: ONE item — `- the user has approved a proposed bump and changelog for the unit, with any edits applied`. Drop the resume OR branch (Fix 4) AND drop the "no part of that release has been applied yet" exclusion clause: post-#45 a half-applied exclusion in a start condition is the disposition in the wrong home (the checker flags it); the NEVER-re-apply invariant and the error step carry the safety. Finished: six items per Fix 2. Invariants: trim the redo invariant to "**NEVER** re-apply an artifact that already exists. A release commit, an annotated tag, a pushed ref, and a published GitHub release are all durable." (Fix 4); trim the staging invariant to the clause before the semicolon (Fix 9). Delete `#### Completing a release that stopped partway` whole — prose, inventory bash block, notes-file line (Fix 4).
- **+Verify** — declare `**Looping step**`. Start: `- a unit's release has been executed` (trim the not-yet-checked clause). Finished: two items per Fix 2.
- **+Finish** — declare `**Success step**`. Start: two items per Fix 1 (every chosen unit released-and-verified, declined, or reported "nothing to release"; no surveyed unit still awaiting the user's choice) — then a new alternative block: `**OR these are true:**` / `- the user has ended the run at the error step`. Finished: summary presented; `- the skill is complete`. Slot deleted. Engagement: gains the Fix 1 clause (list surveyed-but-unselected units with their pending commit counts) and one line for the abandoned entry — the summary states which units released and which remain.
- **+Handle a Problem** — declare `**Error step**`. Start: list, split into OR blocks in the pilot's shape — something has gone wrong (keep the examples: a verification mismatch, a failed command mid-release, an unresolvable config, an unresolvable unit path, an unexpected repository state) OR a situation no other step covers. Finished: two items. Slot deleted. Engagement: default disposition becomes "End the skill. The user repairs the state, then re-runs for a clean pass." (Fix 12); add a `#### Half-applied release` sub-heading carrying the inventory block moved out of `+Execute` (the four commands, plus reading manifest and changelog) as accounting for the user — report exactly which artifacts exist and which do not, name the manual commands that would complete it without running them, end the run, suggest an issue against this skill; fold the existing "A pushed commit and tag are durable … never retry by re-tagging" line into that sub-heading. `#### Github release failure` stays as-is (it is the claim-the-remainder case and keeps the run completing).

## Citations (Fix 5)

Convert per the plan's list: `Preflight` → `[Preflight](#preflight)` (three sites), `Request` → `[Request](#request)`, `Bump Mapping`/`Semver`/`Changelog Rules` → heading links, `assets/CHANGELOG-template.md` → `[CHANGELOG Template](assets/CHANGELOG-template.md)` (two sites), `references/config-template.md` → `[Config Template](references/config-template.md)`. Mini-heading citations (`Git Status`, `Branch`, `GitHub Authenticated`, `Today's Date`) stay code-spans.

## Exemplar

Nominate two: **Looping step** (`+Breaking Changes` or `+Propose`, migrated) and **Dormant step** (`+Setup`, migrated).
