# Target: extensions/agents/course-researcher.md

Agent document, 4 steps, 1 slot, ~8 prose condition blocks, one de-hold-shaped clause per the inherited-scope count. Full common checklist applies.

Specifics:

- **Preamble** — universal swap; verify zero retired vocabulary after ("error drain", "problem step").
- **Condition lists** — convert all prose blocks, one look per item, meaning preserved.
- **De-hold** — one expected site; judge per the three classes, record.
- **Slot** — expected HINT; verify the routing is condition-carried before deleting; flag a load-bearing surprise and leave it in place.
- **Functions** — judge each step against the catalogue; a reporting exit that claims failed runs folds the error step per the executor exception; loops declared where re-entrant.
- **Termination** — the exit step's finished conditions state the run's completion (final message emission for an agent document).
- **Citations** — References-heading citations become links.

## Exemplar

None required.
