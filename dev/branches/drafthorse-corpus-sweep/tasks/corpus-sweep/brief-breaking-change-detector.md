# Target: extensions/agents/breaking-change-detector.md

Agent document, 3 steps, 1 slot, ~6 prose condition blocks, one de-hold-shaped clause per the inherited-scope count. Full common checklist applies.

Specifics:

- **Preamble** — universal swap; verify zero retired vocabulary after ("error drain", "problem step").
- **Condition lists** — convert all prose blocks, one look per item, meaning preserved.
- **De-hold** — one expected site; judge per the three classes, record.
- **Slot** — expected HINT; verify the routing is condition-carried before deleting; flag a load-bearing surprise and leave it in place.
- **Functions** — judge each step; a reporting exit claiming failed runs folds the error step per the executor exception; termination stated in its finished conditions (final message emission).
- **Citations** — References-heading citations become links.

## Exemplar

None required.
