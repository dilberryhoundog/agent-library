# Target: extensions/agents/doc-reviewer.md

Agent document, 4 steps, 2 slots, ~8 prose condition blocks, one de-hold-shaped clause per the inherited-scope count. Full common checklist applies.

Specifics:

- **Preamble** — universal swap; "error drain" clears with it; verify zero retired vocabulary after ("problem step" too).
- **Condition lists** — convert all prose blocks, one look per item, meaning preserved.
- **De-hold** — one expected site; judge per the three classes, record.
- **Slots** — both expected HINT; verify the routing is condition-carried before each deletion; a load-bearing surprise gets flagged and left in place.
- **Functions** — judge each step: a reporting exit that claims failures folds the error step per the executor exception (this document's `+Report`-like step already models the "as the final message text" termination — keep that phrasing); loops declared where re-entrant.
- **Termination** — the reporting step's finished conditions state final-message emission and completion.
- **Citations** — References-heading citations become links.

## Exemplar

None required.
