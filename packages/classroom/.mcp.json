{
  "mcpServers": {
    "classroom-pdf": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp/server.js"],
      "env": {
        "NODE_PATH": "${CLAUDE_PLUGIN_DATA}/node_modules",
        "PUPPETEER_CACHE_DIR": "${CLAUDE_PLUGIN_DATA}/puppeteer"
      }
    }
  }
}
